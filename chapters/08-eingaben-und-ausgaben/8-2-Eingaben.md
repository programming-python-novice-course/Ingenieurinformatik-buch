# Eingaben 

Sie haben bereits die Eingabe über input() in Python verwendet.

```python
text = input("Eingabe: ")
```


In der Praxis ist das schwarze Terminal aber eher unbeliebt. Deshalb stellt man Endanwender meist eine grafische Nutzeroberfläche zur Verfügung. Die Kernidee ist hierbei gleich: wir müssen Information über Schnittstellen hinweg kommunzieren.

In diesem Kapitel sehen wir uns nochmal kurz die Python-built-in Funktion input() an.
Anschließend geben wir einen Ausblick wie man in der Praxis tatsächlich arbeitet (nicht prüfungsrelevant, aber für die Praxis unerlässlich)


