---
jupytext:
  text_representation:
    extension: .md
    format_name: myst
kernelspec:
  display_name: Python 3
  language: python
  name: python3
---

# Übungsaufgabe

```{exercise} Praxisaufgabe (PA1.4): Kreisberechnung mit `math`
:label: ex-paufgaben-a14-kreis-math

Schreiben Sie ein kleines Skript, das nach Eingabe eines Radius $r$ den **Umfang** $u$ und die **Fläche** $A$ berechnet und ausgibt:

- $u = 2\pi r$
- $A = \pi r^2$

**Anforderung:** Nutzen Sie dafür das Modul `math` (z.B. `import math` und dann `math.pi`).

Zusatz: Falls $r \le 0$ eingegeben wird, geben Sie eine Fehlermeldung aus.
```

```{code-cell} python3
import math

```