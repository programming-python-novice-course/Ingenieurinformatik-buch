## Version 2.6.3 - 2026-02-24

### Changed
- Removed the visual separator line between content and footer via footer border override in `/_static/theme-overwrite.css`.


## Version 2.6.2 - 2026-02-24

### Changed
- Extended documentation with architecture/interface dependency details.
  - Added `/_docs/architecture.md`.
  - Added `/_docs/figs/architecture.png`.

## Version 2.6.1 - 2026-02-24

### Changed
- minor: Updated wording in `intro.md` ("Kofferraum packen").
- minor: Updated wording in `chapters/01-course-overview/1-1-Arbeitsmaterialien.md`.
- Removed duplicate images from the repository root.
- Added footer bottom padding to prevent overlap between the HM logo/footer content and fixed action buttons.

## Version 2.6 - 2026-02-22

### Changed
- Launch and Live-Code controls:
  - Replace default launch dropdown behavior with a custom controls bar (`📓`, `▶`/`A`, `⚙`).
  - Add user settings panel for:
    - Jupyter notebook execution environment (`Datahub HM` / `Binder`)
    - Live Code activation mode (`manual` / `auto`)
  - Keep settings persisted in browser storage (`localStorage`).
  - Default settings:
    - JupyterHub selection: `Datahub HM`
    - Live Code mode: `manual`
  - Keep launch controls visible and represent unavailable states as disabled buttons (instead of hiding controls).
  - Use unified green active styling for both `📓` and `▶`; disabled states remain grey.
  - Use `A` (disabled) badge on pages where live code is in automatic mode and executable content is available.

- Launch behavior:
  - Show `📓` as enabled only when a valid launch target is available (Binder or Datahub link found on page).
  - Keep `📓` disabled when no launch target exists.
  - Update launch target dynamically when hub preference changes in settings.

- Live Code behavior:
  - In `manual` mode, show `▶` enabled only when a launch target is available.
  - In `auto` mode, disable `▶`; auto-bootstrap live code on page load.
  - Restrict auto-bootstrap attempts to pages with launch targets to avoid unnecessary load on Binder/Datahub.
  - Ensure hub switch does not interrupt an already running live-code session.

- UI/UX details:
  - Upgrade settings button from text to larger gear icon (`⚙`).
  - Upgrade launch action from text button to icon (`📓`).
  - Upgrade manual live-code action from text button to icon (`▶`).
  - Close settings panel on click outside and on `Escape`.
  - Improve accessibility labels/tooltips for control states.

- Stability and performance:
  - Prevent mutation-observer feedback loops in `user-settings.js`.
  - Throttle DOM refresh work via animation-frame scheduling to avoid timeout-related page stalls.

- Documentation alignment (content update pending in chapter text):
  - Planned wording/UI alignment in:
    - `chapters/01-course-overview/1-1-1-Live-Code.md`
    - `chapters/01-course-overview/1-1-2-Python-Beispiele.md`
    - `chapters/01-course-overview/1-1-3-Live-Code-Alternative-mit-Debugging.md`


## Version 2.5.7 – 2026-02-22

### Added
- Include documentation for build dependencies (Docker images) to docs
- Add robust navigation shortcuts for Jupyter Book:
  - Desktop: Alt+← / Alt+→ for previous/next page
  - Mobile: swipe right/left for previous/next page (main-content-only, editor-safe)

## Version 2.5.6 – 2026-02-22

### Changed
- decide to allow executed cells in print version
- Clean jupyter notebooks: remove kernel header from files without code cells
- Rename chapter files to match their chapter titles and numbering (derived from `_toc.yml`).
- remove unnecessary directories: figs/archive and chapters/archive
- Rename build/dependency folders for clarity:
  - `docker/` → `dev-image/` → `_dev-image/`
  - `scripts/` → `_scripts/`
  - `docs/` → `_docs/`
- Update documentation under `_docs/` to consistently reference **dev-image** and the new folder paths.
- Update CI (`.gitlab-ci.yml`) to use `_scripts/...`.
- Normalize MyST code cell directives: replace {code-cell} ipython3 with {code-cell} python3 across chapter sources.

### Removed
- Cleanup: removed legacy material in `chapters/archive/` and the unused `admonition.txt`.

### Fixed
- Ignore local build artifacts in git: `_website_html/` and `_book_as_pdf/`.

## Version 2.5.5 – 2026-02-20

### Fixed 
 - Fix link redirection - apperently not correnlty merged in v2.5.4


## Version 2.5.4 – 2026-02-19

### Fixed 
 - Remove old link to github on page Expertwissen/intro, redirect to Downloads

## Version 2.5.3 – 2026-02-19

### Fixed
- Typos
- Replaced figures
- Added links

## Version 2.5.2 – 2026-02-18
