image: gitlab.lrz.de:5005/fk03ingenieurinformatik/ingenieurinformatik-buch:latest

variables:
  DOCKER_DRIVER: overlay2

stages:
  - build
  - deploy

# Avoid duplicate pipelines for the same commit (e.g. push + MR pipeline).
# Prefer MR pipelines when a merge request is open; otherwise run normal branch pipelines.
workflow:
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
    - if: '$CI_PIPELINE_SOURCE == "push" && $CI_OPEN_MERGE_REQUESTS'
      when: never
    - if: '$CI_PIPELINE_SOURCE == "push"'
    - if: '$CI_PIPELINE_SOURCE == "web"'
    - when: never

# ============================================================================
# BUILD JOBS
# ============================================================================

build_website_html:
  stage: build
  variables:
    HTML_BUILD_DIR: "_website_html"
    PYDEVD_DISABLE_FILE_VALIDATION: "1"
  before_script:
    # Fix permissions for CI_PROJECT_DIR so non-root user can write
    # This is safer than running all commands as root
    - chown -R $(id -u jovyan):$(id -g jovyan) "$CI_PROJECT_DIR" || true
  script:
    - export PYDEVD_DISABLE_FILE_VALIDATION=1
    - export MPLCONFIGDIR=/tmp/matplotlib
    - gosu jovyan bash -c "rm -rf _build $HTML_BUILD_DIR"
    - gosu jovyan bash -c "jupyter-book build . --path-output $HTML_BUILD_DIR --verbose"
  artifacts:
    paths:
      - $HTML_BUILD_DIR/_build
    when: always

build_book_pdf:
  stage: build
  rules:
    - if: '$CI_COMMIT_BRANCH == "master"'
      when: on_success
    - when: manual
      allow_failure: true
  variables:
    PDF_BUILD_DIR: "_book_as_pdf"
    PYDEVD_DISABLE_FILE_VALIDATION: "1"
  before_script:
    # Fix permissions for CI_PROJECT_DIR so non-root user can write
    # This is safer than running all commands as root
    - chown -R $(id -u jovyan):$(id -g jovyan) "$CI_PROJECT_DIR" || true
  script:
    - export PYDEVD_DISABLE_FILE_VALIDATION=1
    - export MPLCONFIGDIR=/tmp/matplotlib
    - |
      gosu jovyan sh -c 'find figs -name "*.gif" -print0 | while IFS= read -r -d "" gif; do
        png="${gif%.gif}.png"
        if [ ! -f "$png" ]; then
          convert "${gif}[0]" "$png" && echo "generated: $png"
        fi
      done'
    - gosu jovyan bash -c "export MPLCONFIGDIR=/tmp/matplotlib && export LATEXMKOPTS='-interaction=nonstopmode' && jupyter-book build . --builder pdflatex --path-output $PDF_BUILD_DIR --verbose"
  artifacts:
    paths:
      - $PDF_BUILD_DIR/_build/latex/book.pdf
      - $PDF_BUILD_DIR/_build/latex/book.log
    expire_in: 1 week
    when: always

build_book_pdf_print:
  stage: build
  rules:
    - when: manual
      allow_failure: true
  variables:
    PDF_PRINT_BUILD_DIR: "_book_as_pdf_print_version"
    PYDEVD_DISABLE_FILE_VALIDATION: "1"
  before_script:
    # Fix permissions for CI_PROJECT_DIR so non-root user can write
    # This is safer than running all commands as root
    - chown -R $(id -u jovyan):$(id -g jovyan) "$CI_PROJECT_DIR" || true
  script:
    - export PYDEVD_DISABLE_FILE_VALIDATION=1
    - export MPLCONFIGDIR=/tmp/matplotlib
    - gosu jovyan bash -c "rm -rf _build $PDF_PRINT_BUILD_DIR"
    - gosu jovyan python3 scripts/generate_toc_print.py --input _toc.yml --output _toc_print.yml --stop-after chapters/misc/Expertenwissen/0-intro
    - |
      gosu jovyan sh -c 'find figs -name "*.gif" -print0 | while IFS= read -r -d "" gif; do
        png="${gif%.gif}.png"
        if [ ! -f "$png" ]; then
          convert "${gif}[0]" "$png" && echo "generated: $png"
        fi
      done'
    - gosu jovyan bash -c "export MPLCONFIGDIR=/tmp/matplotlib && export LATEXMKOPTS='-interaction=nonstopmode' && jupyter-book build . --toc _toc_print.yml --builder pdflatex --path-output $PDF_PRINT_BUILD_DIR --verbose"
  artifacts:
    paths:
      - $PDF_PRINT_BUILD_DIR/_build/latex/book.pdf
      - $PDF_PRINT_BUILD_DIR/_build/latex/book.log
      - _toc_print.yml
    expire_in: 1 week
    when: always

# ============================================================================
# DEPLOYMENT JOBS
# ============================================================================

deploy_pdf_in_github:
  image: alpine/git:latest
  stage: deploy
  rules:
    - if: '$CI_COMMIT_BRANCH == "master"'
      when: on_success
    - when: manual
      allow_failure: true
  needs:
    - job: build_book_pdf
      artifacts: true
  dependencies:
    - build_book_pdf
  variables:
    PDF_BUILD_DIR: "_book_as_pdf"
  script:
    - |
      VERSION="$(awk -F: '/^[[:space:]]*release:[[:space:]]*/{gsub(/"/,"",$2); gsub(/^[[:space:]]+|[[:space:]]+$/,"",$2); print $2; exit}' _config.yml)" && [ -n "$VERSION" ] || VERSION="unknown"
    - PDF_SOURCE="$PDF_BUILD_DIR/_build/latex/book.pdf"
    - PDF_FILENAME="Skript-${VERSION}.pdf"
    - GITHUB_DOWNLOAD_REPO="fk03ingenieursinformatik/ingenieurinformatik-download"
    - GITHUB_DOWNLOAD_BRANCH="main"
    - GITHUB_REMOTE_PUBLIC="https://github.com/${GITHUB_DOWNLOAD_REPO}.git"
    - GITHUB_REMOTE_PUSH="https://x-access-token:${GITHUB_DOWNLOAD_REPO_TOKEN}@github.com/${GITHUB_DOWNLOAD_REPO}.git"
    - rm -rf _github_download_repo
    - git clone --depth 1 --branch "$GITHUB_DOWNLOAD_BRANCH" "$GITHUB_REMOTE_PUBLIC" _github_download_repo
    - cd _github_download_repo
    - git remote set-url origin "$GITHUB_REMOTE_PUSH"
    - mkdir -p Downloads
    - cp "../${PDF_SOURCE}" "Downloads/${PDF_FILENAME}"
    - cp "../${PDF_SOURCE}" "Downloads/Skript-aktuell.pdf"
    - git add "Downloads/${PDF_FILENAME}" "Downloads/Skript-aktuell.pdf"
    - |
      if git diff --staged --quiet; then
        echo "No changes to push (PDF unchanged)"
      else
        git -c user.name="gitlab-ci" -c user.email="gitlab-ci@localhost" commit -m "Update Skript PDF ${VERSION} from ${CI_PROJECT_PATH}@${CI_COMMIT_SHA}"
        git push origin "$GITHUB_DOWNLOAD_BRANCH"
      fi

deploy_print_pdf_in_github:
  image: alpine/git:latest
  stage: deploy
  rules:
    - when: manual
      allow_failure: true
  needs:
    - job: build_book_pdf_print
      artifacts: true
  dependencies:
    - build_book_pdf_print
  variables:
    PDF_PRINT_BUILD_DIR: "_book_as_pdf_print_version"
  script:
    - |
      VERSION="$(awk -F: '/^[[:space:]]*release:[[:space:]]*/{gsub(/"/,"",$2); gsub(/^[[:space:]]+|[[:space:]]+$/,"",$2); print $2; exit}' _config.yml)" && [ -n "$VERSION" ] || VERSION="unknown"
    - PDF_PRINT_SOURCE="$PDF_PRINT_BUILD_DIR/_build/latex/book.pdf"
    - PDF_PRINT_FILENAME="Skript-print-${VERSION}.pdf"
    - GITHUB_DOWNLOAD_REPO="fk03ingenieursinformatik/ingenieurinformatik-download"
    - GITHUB_DOWNLOAD_BRANCH="main"
    - GITHUB_REMOTE_PUBLIC="https://github.com/${GITHUB_DOWNLOAD_REPO}.git"
    - GITHUB_REMOTE_PUSH="https://x-access-token:${GITHUB_DOWNLOAD_REPO_TOKEN}@github.com/${GITHUB_DOWNLOAD_REPO}.git"
    - rm -rf _github_download_repo
    - git clone --depth 1 --branch "$GITHUB_DOWNLOAD_BRANCH" "$GITHUB_REMOTE_PUBLIC" _github_download_repo
    - cd _github_download_repo
    - git remote set-url origin "$GITHUB_REMOTE_PUSH"
    - mkdir -p Downloads
    - cp "../${PDF_PRINT_SOURCE}" "Downloads/${PDF_PRINT_FILENAME}"
    - cp "../${PDF_PRINT_SOURCE}" "Downloads/Skript-print-aktuell.pdf"
    - git add "Downloads/${PDF_PRINT_FILENAME}" "Downloads/Skript-print-aktuell.pdf"
    - |
      if git diff --staged --quiet; then
        echo "No changes to push (print PDF unchanged)"
      else
        git -c user.name="gitlab-ci" -c user.email="gitlab-ci@localhost" commit -m "Update Skript print PDF ${VERSION} from ${CI_PROJECT_PATH}@${CI_COMMIT_SHA}"
        git push origin "$GITHUB_DOWNLOAD_BRANCH"
      fi

deploy_notebooks_in_github:
  image: alpine/git:latest
  stage: deploy
  rules:
    - if: '$CI_COMMIT_BRANCH == "master"'
      when: on_success
    - when: manual
      allow_failure: true
  needs:
    - job: build_website_html
      artifacts: true
  dependencies:
    - build_website_html
  script:
    - SOURCE_DIR="_website_html/_build/jupyter_execute/chapters"
    - GITHUB_DEPLOY_REPO="fk03ingenieursinformatik/ingenieurinformatik-buch-deploy"
    - GITHUB_DEPLOY_BRANCH="master"
    - TARGET_DIR="deployed_notebooks"
    - GITHUB_REMOTE_PUBLIC="https://github.com/${GITHUB_DEPLOY_REPO}.git"
    - GITHUB_REMOTE_PUSH="https://x-access-token:${GITHUB_DEPLOY_TOKEN}@github.com/${GITHUB_DEPLOY_REPO}.git"
    - rm -rf _github_deploy_repo
    - git clone --depth 1 --branch "$GITHUB_DEPLOY_BRANCH" "$GITHUB_REMOTE_PUBLIC" _github_deploy_repo
    - cd _github_deploy_repo
    - git remote set-url origin "$GITHUB_REMOTE_PUSH"
    - echo "Syncing notebooks into ${TARGET_DIR}/ ..."
    - rm -rf "$TARGET_DIR"
    - mkdir -p "$TARGET_DIR"
    - cp -a "../${SOURCE_DIR}/." "$TARGET_DIR/"
    - |
      VERSION="$(awk -F: '/^[[:space:]]*release:[[:space:]]*/{gsub(/"/,"",$2); gsub(/^[[:space:]]+|[[:space:]]+$/,"",$2); print $2; exit}' ../_config.yml)" && [ -n "$VERSION" ] || VERSION="unknown"
    - |
      printf "%s\n" "Diese notebooks wurden automatisiert erzeugt. Der dazugehörige commit ist: https://gitlab.lrz.de/fk03ingenieurinformatik/Ingenieurinformatik-buch/-/commit/${CI_COMMIT_SHA} (Release: ${VERSION})" > "$TARGET_DIR/Version.md"
    - git add -A
    - git -c user.name="gitlab-ci" -c user.email="gitlab-ci@localhost" commit -m "Update executed notebooks from ${CI_PROJECT_PATH}@${CI_COMMIT_SHA}"
    - echo "Pushing to GitHub..."
    - git push origin "$GITHUB_DEPLOY_BRANCH"

update_website:
  stage: deploy
  rules:
    - if: '$CI_COMMIT_BRANCH == "master"'
      when: on_success
    - when: never
  needs:
    - job: build_website_html
      artifacts: true
  dependencies:
    - build_website_html
  script:
    - 'echo "Pages accessible through ${CI_PAGES_URL}"'
  artifacts:
    paths:
      - _website_html/_build/html
  pages:
    publish: "_website_html/_build/html"

update_website_test:
  stage: deploy
  rules:
    - if: '$CI_COMMIT_BRANCH != "master"'
      when: manual
      allow_failure: true
    - when: never
  needs:
    - job: build_website_html
      artifacts: true
  dependencies:
    - build_website_html
  script:
    - 'echo "Pages accessible through ${CI_PAGES_URL}"'
  artifacts:
    paths:
      - _website_html/_build/html
  pages:
    publish: "_website_html/_build/html"
    path_prefix: "test"
